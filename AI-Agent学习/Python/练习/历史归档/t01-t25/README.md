# t01～t25 基础练习归档

2026-09-21按用户要求整理：27个Python脚本与3个配套JSON文件移入此目录，保留文件名、同目录模块导入和 `data/` 相对位置，代码内容不变。t10和t18各包含入口与辅助模块，因此25个编号共27个脚本。

本目录用于回顾与查证，不默认作为当前教学任务。归档不表示全部独立掌握或全部运行通过；t25的已知NameError仍保留，实际能力和遗留问题见[学习档案](../../../../学习档案.md)。当前课程入口是[C00任务](../../../../任务/C00-文档检索综合诊断.md)。

| 练习范围 | 内容与入口 |
| --- | --- |
| t01～t04 | 条件、循环与成绩统计，从[t01](t01_score_pass_judgment.py)开始 |
| t05～t07 | 文本清洗、词频和JSON，从[t05](t05_text_cleaning.py)开始 |
| t08～t10 | 输入异常、函数和模块，入口[t08](t08_safe_score_input.py)、[t10](t10_run_score_report.py) |
| t11～t16 | JSON文件、入口、数据校验和错误处理，从[t11](t11_score_report_json.py)开始 |
| t17～t18 | 校验函数和模块拆分，入口[t17](t17_validate_scores_function.py)、[t18](t18_run_score.py) |
| t19～t23 | 字典记录、更新、列表处理和函数统计，从[t19](t19_score_record_dict.py)开始 |
| t24～t25 | 函数重新实现与统计，入口[t24](t24_function_reimplementation.py)、[t25](t25_score_summary.py) |

如需复查，进入本目录后直接运行对应脚本，例如 `python -B t18_run_score.py`；也可直接传脚本的完整路径。保持同目录导入方式，不改成从父目录按包路径运行。部分旧脚本会交互或写入配套JSON，查阅源码无需执行；本次归档未运行这些脚本。

旧文档快照或聊天中提及的 `Python/练习/tXX_*.py`，现在对应本目录的同名文件；历史原文不因此改写。
