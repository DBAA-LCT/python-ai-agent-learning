# 学习档案

> 本文件是学习进度与真实能力的事实来源。只根据实际练习结果更新，不根据“看过”或“听懂了”判定掌握。

## 基本信息

- 学习目标：在职转型为 Python + AI Agent 应用开发工程师
- 城市优先级：南京 → 杭州 → 上海
- 可用时间：工作日约 1～2 小时；周末每天约 3～5 小时
- 建档日期：2026-09-16

## 当前阶段

- 阶段：Phase 1 — Python 编程能力
- 历史基线：Day 1 已完成
- 当前模块：`t24-函数独立重写与返回值`
- 当前目标：独立定义接收分数列表并返回统计结果的函数
- 当前状态：t24 已完成；参数使用、局部计数器、`return` 和主流程调用均已独立实现并通过验证

## 当前能力评估

> 当前没有足够证据给出完整 Python 能力星级。已完成的任务只覆盖局部主题，以下结论均为局部观察，不代表整体能力。

### Python 基础语法

- 窄范围观察：接触过变量、条件、输入、类型转换、列表、循环、计数器、函数、参数、返回值和输出；在提示下修复了类型、变量名和边界条件问题
- 知识理解：待全面评估；模块 02 的代码实现和运行结果已通过，口头解释检查由学习者主动跳过，不作为负面证据
- 独立编码：待全面评估（已独立写出模块 02、04 的程序并通过运行）
- Debug：待全面评估（模块 01 在提示下完成了几类错误定位和修复）
- 判断依据：模块 01、02、04 的小程序已运行通过；没有覆盖文件、模块、综合拆解或完全无提示测试

### 算法

- 状态：基础较弱，尚未评估

### 后端 / LLM / RAG / Agent

- 状态：尚未进入系统学习阶段

## 已掌握知识

- 暂无经过独立、重复或变式练习验证的稳定掌握记录
- 已接触并完成过局部练习：`input()` 输入、`int()` 类型转换、`if/else` 条件判断、`>=` 边界条件、列表、`append()`、`for` 循环、计数器和 `print()` 输出

## 正在学习

- Python 基础能力的分层评估
- 函数参数、返回值和职责拆分
- 字符串清理、分割、拼接与统计
- 需求拆解：识别输入、处理规则和输出
- 读懂 traceback 并根据报错定位问题

## 薄弱知识

- 目前证据不足，不能把“有基础”或“完成一个小题”直接等同于整体掌握程度
- `input()` 返回值的类型与类型转换结果是否保存
- 变量定义、变量名一致性
- `>` 与 `>=` 的边界差异
- Debug 能力（模块 01 为有提示修复，待无提示和变式复查）
- 文件、模块等尚未评估
- 算法基础（依据自述）

## 已完成练习

- Day 1 / 模块 01：根据分数输出“及格”或“不及格”——窄范围基础语法探针
  - 文件：`AI-Agent学习/Python/练习/t01_score_pass_judgment.py`
  - 实测结果：`59` → `不及格`；`60` → `及格`；`85` → `及格`
  - 评估范围：只覆盖输入、整数转换、变量使用、简单条件判断和输出；不能据此判断整体 Python 水平

- 模块 02：处理 5 个成绩并统计及格人数
  - 文件：`AI-Agent学习/Python/练习/t02_score_pass_count.py`
  - 实际实现：使用列表收集 5 个整数成绩，遍历列表并使用计数器统计 `>= 60` 的成绩
  - 实测结果：`59、60、85、90、30` → `及格的人数为：3`
  - 验证方式：使用 Python 3.14 实际运行，退出码为 0
  - 完成范围：代码结构和运行结果已验证；学习者主动跳过口头解释检查以节省时间，不把跳过解释记为不会

## 当前练习

- 练习 24：函数独立重写与返回值
- 文件：`AI-Agent学习/Python/练习/t24_function_reimplementation.py`
- 实际实现：定义函数接收整数分数列表，统计分数 `>= 80` 的人数并使用 `return` 返回；`main()` 负责准备数据、调用函数和输出。
- 验证结果：`py_compile` 通过；直接运行输出 `优秀人数：2`。
- 能力说明：本次由学习者独立完成核心逻辑，教练直接修正了类型标注、函数命名和局部变量命名。

## 错误记录摘要

- 2026-09-16：`input()` 得到字符串却直接与整数比较
- 2026-09-16：修改后使用了未定义的 `score`，产生 `NameError`
- 2026-09-16：使用 `>` 导致 60 未被判定为及格，后改为 `>=`
- 详细记录见 `错误档案.md`

## 当前项目

- 暂无。完成 Python 基础能力训练后再逐步建立项目。

## 学习组织方式

- `Day 1` 仅作为首次基线探针的历史记录保留。
- 后续不按日期命名，改用“模块序号 + 主题”表示学习顺序。
- 当前基础 Python 顺序暂定为：
  1. `01-输入与条件`
  2. `02-循环与计数`
  3. `03-列表与聚合`
  4. `04-函数与拆分`
  5. `05-字符串处理`
  6. `06-字典与数据建模`
  7. `07-文件与 JSON`
  8. `08-异常、模块与工程习惯`
- 顺序是学习路线，不代表每天完成一个模块；每个模块可根据实际掌握情况拆成多个小任务或回退复习。
- 练习文件命名规则：使用 `t01_...`、`t02_...` 这样的字母加序号格式，既保留学习顺序，也保证文件名可作为 Python 模块导入。
- 每次开始明确标注本次属于：基线评估、针对性教学或变式复习，三者不混称。

## 历史任务记录

- 练习编号：`t14`
- 类型：针对性教学
- 目标：为 JSON 读取后的成绩列表增加基本数据校验，区分“文件读取成功”和“数据内容有效”
- 重点：空列表、非列表 JSON 数据和异常处理边界；不重复实现统计函数
- 文件：`t14_validate_json_scores.py`

## 代码审查标准

后续代码审查按“大厂工程实践”分层执行，但不把生产项目的复杂要求强行套在基础练习上。

### 练习代码

1. **需求正确性（P0）**：输入、处理、输出是否符合要求；边界条件是否正确。
2. **可读性（P1）**：变量和函数命名准确，控制流程清楚，避免误导性写法。
3. **基本健壮性（P1）**：类型转换、变量生命周期、空数据和明显异常是否考虑；超出当前任务范围的问题单独标注，不擅自扩大任务。
4. **验证意识（P1）**：是否有代表性测试，尤其是边界值；能否根据实际运行结果判断正确性。
5. **风格规范（P2）**：格式统一、拼写正确、避免硬编码和不必要的重复。

### 小型脚本

在练习标准之上，增加函数拆分、输入校验、异常处理、类型标注、可测试性、复杂度和模块边界。

### 生产项目

进一步检查测试覆盖、日志与可观测性、配置管理、依赖安全、权限与数据安全、性能、文档、Git 提交质量和 CI 检查。

### 审查输出格式

每次审查优先给出：结论、阻塞问题、重要改进、可选风格建议、验证结果。先处理会导致错误或维护风险的问题，不把个人偏好伪装成硬性规范。

### React / Next.js 代码专项标准

- React：组件职责、Props/State 设计、Effect 使用、列表 key、表单处理、加载/错误状态、可访问性、TypeScript 类型和组件测试。
- Next.js：Server/Client 边界、路由与布局、数据获取与缓存、错误/加载页面、SEO、认证安全、性能和部署配置。

## 前端学习方向

在 Python + AI 主线之外增加独立的前端轨道，目标是能够使用 React 和 Next.js 构建 AI 应用前端，而不是只会跟着教程拼页面。

### 前端学习顺序

1. `F01-JavaScript与TypeScript基础`：模块、数组对象、函数、异步、Promise、ES Modules、npm 和 TypeScript 基础。
2. `F02-React核心`：组件、Props、State、事件、表单、条件渲染、列表、Hooks 和组件拆分。
3. `F03-React工程实践`：数据请求、状态管理、错误与加载状态、组件复用、测试、代码规范和性能基础。
4. `F04-Next.js应用开发`：App Router、布局、路由、Server/Client Components、数据获取、缓存、表单和错误处理。
5. `F05-AI应用前端整合`：将 React/Next.js 前端与 FastAPI、LLM、RAG 或 Agent 后端连接，处理流式输出和交互状态。

### 前端执行方式

- 当前继续完成 Python 模块 `03-列表与聚合`，不在一个小任务中同时切换多个技术栈。
- Python 基础推进到函数和模块后，启动 `F01`；前端作为并行轨道逐步学习。
- 在 React 之前先补足 JavaScript/TypeScript 必要基础，不直接从 Next.js 模板开始。
- 每个前端模块同样按需求、实现、运行、测试和代码审查推进。

### 需求实现的优雅性

在正确性通过后，单独审查实现是否是当前需求下的合适解法：

1. **需求映射**：代码结构是否自然对应需求步骤，是否存在无关逻辑。
2. **抽象层级**：是否既没有把简单问题写得过度复杂，也没有把应该复用的逻辑散落重复。
3. **数据流设计**：数据从输入到处理再到输出是否清楚，是否存在不必要的中间状态或重复保存。
4. **语言习惯**：是否合理使用 Python、JavaScript 或 TypeScript 的常见表达方式，而不是机械堆砌语法。
5. **扩展与修改成本**：需求发生小幅变化时，是否只需要改动一个清晰的位置。
6. **学习目标平衡**：练习阶段可以为了训练循环、列表等知识点保留显式实现；不能因为“更短”就跳过本次要练习的核心能力。

“优雅”不是代码越短越好，而是在满足需求、符合当前学习目标的前提下，复杂度、可读性和扩展性之间取得合理平衡。

## 模块 04 完成记录

- 主题：函数与拆分
- 文件：`AI-Agent学习/Python/练习/t04_score_stats.py`
- 实际实现：定义接收 `list[int]` 的 `calculate_stats` 函数，返回总分和平均分；对空列表抛出 `ValueError`；主流程负责输入和输出。
- 代码审查：P0 正确性通过；函数拆分和数据流设计清晰；类型标注、空列表保护和命名达到当前模块要求。
- 实际验证：`70、80、90、60、100` → 总分 `400`、平均分 `80.0`；空列表调用得到 `ValueError: 成绩列表不能为空`。

## 档案维护策略

- 本文件是滚动摘要，不保存完整学习过程。
- 每个模块完成后只记录：模块、文件、实际结果、审查结论和仍需复查的点。
- 详细过程放在对应的每日或模块记录中；约每 3～5 个模块进行一次压缩和去重。
- 压缩时优先保留当前进度、已验证证据、活跃薄弱点、代表性错误、长期偏好和下一步任务。

## 模块 05 完成记录

- 主题：字符串处理
- 文件：`AI-Agent学习/Python/练习/t05_text_cleaning.py`
- 实际实现：使用 `split()` 拆分空白，使用 `" ".join(...)` 统一空格，并用列表长度统计单词数。
- 代码审查：P0 正确；数据流和标准库使用方式清晰；仅有输入提示措辞和教学注释可做风格优化，不阻塞完成。
- 实际验证：普通多余空格、空输入和制表符/连续空格场景均运行通过。

## 模块 08 进度记录

- 主题：异常处理
- 文件：`AI-Agent学习/Python/练习/t08_safe_score_input.py`
- 实际实现：定义 `check_input_score(raw_score)`，在函数内完成字符串转整数、范围校验和 `ValueError` 抛出；`main()` 负责获取输入、调用函数、捕获异常和输出；使用 `if __name__ == "__main__":` 控制直接运行入口。
- 实际验证：输入 `85、0、100` 正常返回成绩；输入 `-1、101` 输出范围错误；输入 `abc` 输出整数错误；六组结果均符合要求，退出码为 0。
- 当前结论：已完成异常处理与函数拆分；学习者已理解 `main()` 与入口判断用于区分直接运行和被导入场景，仍需通过实际导入场景复查。

## 模块 11 学习进展

- 主题：输入异常与用户体验（已完成本次小目标）
- 已理解：`raise` 用于主动抛出异常或继续传播异常；`except ValueError` 用于捕获指定异常；`except ValueError as error` 可取得异常对象并输出提示；`return` 可结束当前 `main()` 流程，阻止无效数据继续执行。
- 已实现：在 `t10_run_score_report.py` 中捕获成绩数量和成绩内容的 `ValueError`，错误提示后结束流程；文件头保留模块 10 说明并补充模块 11 改进说明。
- 验证结果：数量输入 `abc`、数量输入 `0`、成绩输入 `abc` 均正常提示并结束且无 traceback；输入 `2、59、60` 正常输出统计报告；`py_compile` 通过。
- 待复查：在新的“必须大于 0”的数量参数练习中，独立设计 `raise`、`except` 和结束流程。


## 模块 12 完成记录

- 主题：从 JSON 文件读取成绩列表并生成统计报告
- 文件：`AI-Agent学习/Python/练习/t12_score_report_from_json.py`
- 输入文件：`AI-Agent学习/Python/练习/data/t12_score.json`，内容为 JSON 数组 `[59, 60, 80]`
- 实际实现：使用 `Path` 定位 data 目录，使用 `json.load(file)` 将 JSON 数组读取为 Python 列表 `scores`，直接传给 `t10_score_utils.get_score_stats()` 生成报告；对文件不存在情况使用 `try/except FileNotFoundError` 提示用户。
- 代码审查：P0 正确；已修正从 t11 间接导入统计函数的问题，改为从函数定义所在的 `t10_score_utils` 导入；已修正异常处理范围和异常后继续使用未赋值变量的问题。`main()` 与入口判断可作为后续工程习惯改进，但不阻塞本练习目标。
- 验证结果：正常运行输出总人数 `3`、及格人数 `2`、平均分 `66.33333333333333`、最高分 `80`、最低分 `59`；`py_compile` 通过。缺失文件分支根据代码结构审查可正确提示且不会继续统计，未单独执行该变式。

## 历史任务记录：t17

- 练习编号：`t17`
- 类型：针对性教学
- 目标：把成绩数据校验逻辑提取为独立函数，明确函数返回值与异常职责
- 重点：函数参数、返回值、`raise ValueError`、主流程与校验函数的职责边界
- 文件：`t17_validate_scores_function.py`


## 模块 13 完成记录

- 主题：`main()` 与入口判断
- 文件：`AI-Agent学习/Python/练习/t13_score_report_entry.py`
- 实际实现：保留 import 在模块顶层，将 t12 的路径处理、文件读取、异常处理、统计和输出放入 `main()`；使用 `if __name__ == "__main__": main()` 控制直接运行。
- 代码审查：P0 正确；曾出现一次将 `"__main__"` 写成 `"main"` 的笔误，导致条件为假且脚本无输出，已修正，不作为长期代表性错误记录。复习说明已补充到文件头。
- 验证结果：直接运行输出总人数 `3`、及格人数 `2`、平均分 `66.33333333333333`、最高分 `80`、最低分 `59`；`py_compile` 通过；导入模块后只输出测试标记，不自动执行报告流程。


## 模块 14 完成记录

- 主题：JSON 数据结构校验
- 文件：`AI-Agent学习/Python/练习/t14_validate_json_scores.py`
- 实际实现：在 `json.load()` 后、统计前使用 `isinstance(scores, list)` 校验数据类型，使用 `if not scores` 拒绝空列表；无效数据主动抛出 `ValueError`，并分别处理 `FileNotFoundError` 与 `ValueError`。
- 代码审查：P0 正确；正常路径、校验顺序和异常边界符合本次目标。曾将 `isinstance()` 条件方向写反，已根据运行结果定位并修正；曾遗漏 `ValueError` 捕获，已补充。错误提示可补充标点，但不阻塞完成。
- 验证结果：正常数据输出总人数 `3`、及格人数 `2`、平均分 `66.33333333333333`、最高分 `80`、最低分 `59`；`py_compile` 通过；非列表和空列表分支根据代码结构会被捕获且不会继续统计。


## 模块 15 完成记录

- 主题：JSON 列表元素类型校验
- 文件：`AI-Agent学习/Python/练习/t15_validate_score_item.py`
- 实际实现：在列表整体和非空校验之后遍历成绩列表，使用 `isinstance(score, (int, float))` 校验每个元素；无效元素主动抛出 `ValueError`，全部通过后才调用统计函数。
- 代码审查：P0 正确；曾将 `isinstance()` 的类型集合写成列表 `[int, float]`，导致 `TypeError`，已改为类型元组；曾遗漏统计函数导入，已补充；删除了 `raise` 后不可达的 `break`。
- 验证结果：正常数据输出总人数 `3`、及格人数 `2`、平均分 `66.33333333333333`、最高分 `80`、最低分 `59`；`py_compile` 通过；非数字元素分支根据代码结构会在统计前抛出并捕获 `ValueError`。


## 模块 16 完成记录

- 主题：JSON 文件格式错误处理
- 文件：`AI-Agent学习/Python/练习/t16_handle_json_decode_error.py`
- 实际实现：保留 t15 的列表和元素校验，新增 `except json.JSONDecodeError`，并放在 `ValueError` 之前；分别处理文件不存在、JSON 解析失败和数据内容无效。
- 代码审查：P0 正确；曾将 JSON 解析阶段误认为会进入 `isinstance()` 校验，已澄清 `json.load()` 会先抛出 `JSONDecodeError`；曾出现一次 `except` 拼写笔误，已修正，不作为长期错误记录。
- 验证结果：正常数据输出总人数 `3`、及格人数 `2`、平均分 `66.33333333333333`、最高分 `80`、最低分 `59`；`py_compile` 通过；异常顺序和捕获范围符合目标。


## 模块 17 完成记录

- 主题：将成绩数据校验提取为独立函数
- 文件：`AI-Agent学习/Python/练习/t17_validate_scores_function.py`
- 实际实现：定义 `validate_scores(scores)`，校验列表类型、非空和元素是否为 `int` 或 `float`；成功返回原列表，失败抛出 `ValueError`。`main()` 负责 JSON 读取、异常处理、调用校验函数和统计输出；统计函数从 `t10_score_utils` 导入。
- 代码审查：P0 正确；已修复缩进错误、循环变量拼写错误、未定义变量、异常类名和错误模块导入；补充了模块复习说明。
- 验证结果：`py_compile` 通过；正常数据只输出一次统计报告；非列表、空列表、非数字元素均按预期抛出并由主流程处理；导入 t17 不会自动执行 `main()`。

## 模块 19 完成记录

- 主题：使用字典表示一条成绩记录
- 文件：`AI-Agent学习/Python/练习/t19_score_record_dict.py`
- 实际实现：创建包含 `name`、`score` 和 `passed` 的普通字典；读取姓名和成绩；更新成绩后同步更新 `passed` 字段。
- 学习过程：学习者最初将“一条记录”和“多条记录”混淆，经提示后理解普通字典表示一条记录，暂未引入嵌套结构。
- 代码审查：P0 正确；已修复 f-string 引号冲突，并补充成绩低于 60 时将 `passed` 更新为 `False` 的分支。当前写法可运行，但 `passed` 属于由 `score` 推导出的字段，后续可练习用布尔表达式减少重复分支。
- 验证结果：`py_compile` 通过；直接运行输出初始姓名和成绩，最终记录为成绩 `65`、`passed` 为 `True`。
- 能力判断：字典键值、字段读取、添加和更新已在提示后完成；独立编码仍需变式复查。缺失键行为尚未通过独立代码测试。

## 模块 20 完成记录

- 主题：字典缺失键与安全读取
- 文件：`AI-Agent学习/Python/练习/t20_missing_dict_key.py`
- 实际实现：使用 `record["name"]` 读取必填字段；访问不存在的 `"class"` 时捕获 `KeyError`；使用 `record.get("class", "class未设置")` 为可选字段提供默认值。
- 代码审查：P0 正确；已补充模块复习说明。发现了一个未使用的 `getclass` 变量，但不影响结果，作为可读性习惯提醒，未扩大本次练习范围。
- 验证结果：`py_compile` 通过；直接运行输出姓名、缺失字段提示和默认值。
- 能力判断：在提问和概念澄清后，学习者独立完成了两种读取方式和异常处理；该知识点已完成一次变式验证，仍需在多条记录场景中继续观察字典结构理解。

## 模块 21 完成记录

- 主题：列表中的多个字典
- 文件：`AI-Agent学习/Python/练习/t21_student_records.py`
- 实际实现：创建小明和小红两条字典记录，将记录放入列表，遍历列表并使用 `record["name"]`、`record["score"]` 读取字段；使用 `try/except KeyError` 处理字段缺失情况。
- 学习过程：学习者能独立分析出“外层是列表、元素是字典、每条记录包含 name 和 score”，并完成遍历读取。
- 代码审查：P0 正确；修复了 f-string 引号冲突。当前 `try/except` 与 `[]` 读取方式匹配，但本题的正常数据不触发异常，后续可继续练习何时需要异常处理。
- 验证结果：`py_compile` 通过；直接运行输出小明成绩 58、小红成绩 85。
- 能力判断：列表与字典组合、循环遍历和字段读取已在提示后完成；f-string 引号习惯需要无提示变式复查。

## 模块 22 完成记录

- 主题：遍历多条记录并更新字段
- 文件：`AI-Agent学习/Python/练习/t22_update_student_records.py`
- 实际实现：创建三条学生成绩字典记录，将记录放入列表，遍历当前 `record`，根据 `record["score"] >= 60` 添加 `passed` 字段。
- 学习过程：学习者能独立分析循环变量代表当前字典，并正确处理 `58、85、60` 三个成绩。
- 代码审查：P0 正确；`if/else` 清楚表达了 True 和 False 两条路径，符合本次学习目标。后续理解稳定后可复习用布尔表达式简化赋值，但当前无需修改。
- 验证结果：`py_compile` 通过；直接运行输出小明 `False`、小红 `True`、小刚 `True`。
- 能力判断：列表中的字典、循环中的当前元素、字段读取与更新已在提示后完成；仍需通过统计变式检查能否独立维护计数器和边界条件。

## 模块 22 补充完成记录

- 主题：合并字段更新与及格人数统计
- 实际文件：`AI-Agent学习/Python/练习/t22_update_student_records.py`
- 实际实现：在一次遍历中根据每条记录的成绩添加 `passed` 字段，并在及格分支递增 `passed_num`；使用 `58、85、60` 验证不及格、普通及格和边界值。
- 代码审查：P0 正确；一次循环完成字段更新和计数，未引入不必要的第二次遍历。输出裸数字 `2` 不影响功能，后续可改为带标签的输出以提升可读性。
- 验证结果：`py_compile` 通过；直接运行输出三条记录的 `passed` 为 False、True、True，及格人数为 2。
- 能力判断：已在提示后完成列表字典遍历、字段更新、计数器和边界判断；下一步可将统计逻辑提取为函数，复查参数和返回值职责。

## 模块 24 完成记录

- 主题：函数独立重写与返回值
- 文件：`AI-Agent学习/Python/练习/t24_function_reimplementation.py`
- 实际实现：定义函数接收分数列表，使用内部计数器统计分数 `>= 80` 的人数并通过 `return` 返回；`main()` 负责准备数据、调用函数和输出。
- 学习过程：学习者独立完成了参数使用、循环、条件判断、计数器、返回值和入口调用。
- 代码审查：P0 正确；函数数据流和职责符合目标。类型标注已修正为 `list[int]`，函数名已改为 `count_excellent_students`，参数、循环变量和计数器命名与实际规则一致。
- 验证结果：`py_compile` 通过；直接运行 `[59, 60, 80, 95]` 输出 `优秀人数：2`。
- 能力判断：本次已获得函数参数、局部计数器和 `return` 的局部独立编码证据；命名和类型标注由教练修正，不将这部分改动计为独立能力。

## 下一次任务

- 练习编号：`t25`
- 类型：针对性复习
- 唯一目标：复查函数接收空列表时的行为，并明确函数应返回什么结果。
- 重点检查：空列表、计数器初始值、`return`；暂不增加异常处理或新业务逻辑。
- 文件计划：`t25_empty_list_function.py`

